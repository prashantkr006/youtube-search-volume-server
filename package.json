{
  "name": "server",
  "version": "1.0.0",
  "description": "server for search feature",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client": "cd client && npm start",
    "dev": "npm run start",
    "heroku-postbuild": "cd client && npm install --only=dev && npm run build"
  },
  "author": "prashant",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
